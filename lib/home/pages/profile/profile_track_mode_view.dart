import 'package:flutter/material.dart';

class ProfileTrackModeView extends StatelessWidget {
  const ProfileTrackModeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
import 'package:flutter/material.dart';

class QuizOnboradingView extends StatelessWidget {
  const QuizOnboradingView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}